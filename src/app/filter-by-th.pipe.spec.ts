import { FilterByThPipe } from './filter-by-th.pipe';

describe('FilterByThPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByThPipe();
    expect(pipe).toBeTruthy();
  });
});
